[pytest]
markers =
    integration: marks tests as integration tests that require external network or build tools
    slow: marks tests that take tens of seconds (full LangGraph + model runs); excluded from the fast CI job
