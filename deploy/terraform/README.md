# 🏗️ Terraform Infrastructure as Code (IaC) — CreditFlow

Quản lý hạ tầng Azure Container Apps cho CreditFlow bằng Terraform:

```bash
cd deploy/terraform
terraform init
terraform plan
terraform apply -auto-approve
```
